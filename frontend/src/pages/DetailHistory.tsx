import React, { useEffect } from "react";
import { useParams } from "react-router";

const DetailHistory = () => {
  const { id } = useParams();

  useEffect(() => {}, []);
  return <div></div>;
};

export default DetailHistory;
